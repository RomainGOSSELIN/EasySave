<Window x:Class="EasySaveWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Client WPF" Height="350" Width="525">
    <DataGrid x:Name="DataGridBackupJobs" AutoGenerateColumns="False" VerticalAlignment="Center" HorizontalAlignment="Center">
        <DataGrid.Resources>
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="#EEEEEE"/>
                <Setter Property="RowBackground" Value="White"/>
                <Setter Property="AlternatingRowBackground" Value="#FAFAFA"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
            </Style>
        </DataGrid.Resources>
        <DataGrid.Columns>
            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*"/>
            <DataGridTextColumn Header="Nom" Binding="{Binding BackupName}" Width="*"/>
            <DataGridTextColumn Header="Source" Binding="{Binding SourceFilePath}" Width="*"/>
            <DataGridTextColumn Header="Destination" Binding="{Binding TargetFilePath}" Width="*"/>
            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
            <DataGridTextColumn Header="Progression (%)" Binding="{Binding Progress}" Width="*"/>
        </DataGrid.Columns>
    </DataGrid>

</Window>
